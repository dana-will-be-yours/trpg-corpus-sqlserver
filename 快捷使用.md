# 快捷使用

本檔適用 `v1.4` 分支。`main` 分支不在本次修正範圍內，避免影響資料維護。

環境預設：SQL Server Management Studio 22、SQL Server T-SQL、資料庫 `TRPG_Corpus_DB`、正式表 `dbo`、暫存表 `stg`。

前端頁面：

```text
web/dago-corpus-input.html
```

v1.4 目前功能：

```text
1. DOCX / 純文字逐字稿匯入。
2. 50,000 rows turbo chunk 匯入。
3. IndexedDB chunk 工作區：dagoCorpusTurboChunkDB_v3。
4. stg.Utterance_Import 預覽分頁。
5. Speaker Mapping 匯入 / 匯出 / 權威套用。
6. row 操作：上插、下插、分割、刪除、欄位修改。
7. JSON / XLSX / UTF-16LE TSV 下載。
8. JSON / XLSX / TSV 分批匯出。
9. 前端驗證後保留 Speaker Mapping 的 speaker_type / speaker_code。
10. SQL Server staging 驗證與正式表載入。
```

## 1. 開啟資料庫

```sql
USE TRPG_Corpus_DB;
GO
```

## 2. 前端 v1.4 基本操作

開啟輸入頁：

```text
web/dago-corpus-input.html
```

若使用 GitHub Pages 測試，網址格式：

```text
https://dana-will-be-yours.github.io/trpg-corpus-sqlserver/web/dago-corpus-input.html?reload=20260517-rowops-v3
```

進頁後用瀏覽器 Console 檢查：

```js
[
  window.DagoCorpusV14TurboChunkV3?.BUILD,
  window.DagoCorpusV14RowOpsV3?.BUILD,
  typeof window.DagoCorpusV14TurboChunkV3?.fastParse,
  typeof window.DagoCorpusWorkspaceOpsV14?.insertRow,
  typeof window.DagoCorpusWorkspaceOpsV14?.deleteRowByNo,
  typeof window.DagoCorpusWorkspaceOpsV14?.splitRowByNo,
  typeof window.DagoCorpusWorkspaceOpsV14?.updateRowByNo,
  document.getElementById('status')?.textContent
]
```

應看到：

```text
20260517-turbo-chunk-v3
20260517-turbo-chunk-v3-rowops
function
function
function
function
function
```

## 3. DOCX 匯入流程

```text
1. 填 project_code、team_code、session_code、batch_code。
2. 選擇 .docx。
3. 按「上傳 Word .docx」。
4. 檢查 status 是否顯示「DOCX turbo chunk v3 匯入完成」。
5. 檢查 workspaceStatus 是否顯示 TURBO_CHUNK、rows、chunks、版本。
6. 檢查 Speaker Mapping 是否產生。
7. 檢查 stg.Utterance_Import 預覽是否可分頁。
```

DOCX turbo chunk v3 匯入後，資料保存在瀏覽器 IndexedDB：

```text
dagoCorpusTurboChunkDB_v3
```

此資料尚未進入 SQL Server。匯入 SQL Server 前，應先下載備份：

```text
下載 JSON
下載 XLSX
下載 UTF-16LE TSV
下載 Mapping JSON
下載 Mapping XLSX
下載 Mapping UTF-16LE TSV
```

## 4. 50,000 rows 效能測試

在 Console 執行：

```js
document.getElementById('sourceText').value = Array.from({ length: 50000 }, (_, i) => {
  const speakers = ['GM', '陽月', '楚服', '花瓊瑤'];
  const sp = speakers[i % speakers.length];
  return `${sp}：第 ${i + 1} 則測試發言，這是用於 v1.4 turbo chunk v3 效能測試的文字。`;
}).join('\n');

await window.DagoCorpusV14TurboChunkV3.fastParse();

[
  await window.DagoCorpusWorkspaceV14.getRowCount(
    window.DagoCorpusWorkspaceV14.getCurrentWorkspaceId()
  ),
  document.getElementById('workspaceStatus')?.textContent,
  document.getElementById('status')?.textContent
]
```

通過標準：

```text
row count = 50000
workspaceStatus 包含 rows=50000；chunks=10；版本=20260517-turbo-chunk-v3
status 顯示 v3 turbo chunk 完成與耗時
```

## 5. row 操作複查

匯入 DOCX 或產生測試資料後，逐項測試：

```text
1. 按某列「上插」，row count 應 +1。
2. 按某列「下插」，row count 應 +1。
3. 在 raw text 中選定位置後按「分割」，row count 應 +1。
4. 按某列「刪除」，row count 應 -1。
5. 修改 speaker_code，離開欄位後應保存。
6. 重新整理頁面後，修改結果仍存在。
7. 顯示發話者下拉選單數量應更新。
8. 下載 JSON，確認插入、分割、刪除後的 rows 已反映在匯出檔。
```

Console 檢查 row 操作 adapter：

```js
[
  window.DagoCorpusV14RowOpsV3?.BUILD,
  typeof window.DagoCorpusWorkspaceOpsV14?.insertRow,
  typeof window.DagoCorpusWorkspaceOpsV14?.deleteRowByNo,
  typeof window.DagoCorpusWorkspaceOpsV14?.splitRowByNo,
  typeof window.DagoCorpusWorkspaceOpsV14?.updateRowByNo
]
```

## 6. Speaker Mapping 使用流程

建議欄位：

```text
raw_speaker_label：逐字稿原始發話者標籤，例如 GM、陽月、楚服。
speaker_type：GM、PL、PC、NPC、Observer、Researcher。
speaker_code：資料庫使用的代碼，例如 GM_MAIN、PC_YANGYUE。
target_table：Team_Member、Player_Character、NPC。
note：備註。
```

範例：

```text
GM       | GM | GM_MAIN        | Team_Member
陽月     | PC | PC_YANGYUE     | Player_Character
楚服     | PC | PC_CHUFU       | Player_Character
花瓊瑤   | PC | PC_HUAQIONGYAO | Player_Character
```

按「套用 Speaker Mapping」後檢查：

```js
(await window.DagoCorpusWorkspaceV14.getRowsPage(
  window.DagoCorpusWorkspaceV14.getCurrentWorkspaceId(),
  1,
  20,
  '__ALL__'
)).map(r => ({
  row: r.source_row_no,
  raw: r.speaker_label_raw,
  type: r.speaker_type,
  code: r.speaker_code
}))
```

通過標準：

```text
raw_speaker_label 對應到 mapping 中指定的 speaker_code。
前端驗證後 speaker_code 不應回到原始 raw label。
```

## 7. 前端匯出到 SQL Server staging

前端匯出建議使用：

```text
下載 XLSX：人工檢查用。
下載 UTF-16LE TSV：SSMS / BULK INSERT 較穩定。
分批 TSV：大量資料匯入 SQL Server 時使用。
下載 Mapping XLSX / TSV：保留發話者對應表。
```

檔名建議：

```text
<batch_code>_stg_Utterance_Import_<yyyymmdd_hhmmss>.tsv
<batch_code>_Speaker_Mapping_<yyyymmdd_hhmmss>.xlsx
```

## 8. 查資料庫物件

```sql
SELECT s.name AS schema_name, t.name AS table_name
FROM sys.tables AS t
INNER JOIN sys.schemas AS s
    ON s.schema_id = t.schema_id
WHERE s.name IN (N'dbo', N'stg')
ORDER BY s.name, t.name;

SELECT s.name AS schema_name, p.name AS procedure_name
FROM sys.procedures AS p
INNER JOIN sys.schemas AS s
    ON s.schema_id = p.schema_id
WHERE s.name IN (N'dbo', N'stg')
ORDER BY s.name, p.name;

SELECT s.name AS schema_name, v.name AS view_name
FROM sys.views AS v
INNER JOIN sys.schemas AS s
    ON s.schema_id = v.schema_id
WHERE s.name IN (N'dbo', N'stg')
ORDER BY s.name, v.name;
```

## 9. 查研究專案與隊伍

```sql
SELECT project_id, project_code, project_title, research_design, data_status
FROM dbo.Research_Project
ORDER BY project_id;

SELECT
    rp.project_code,
    t.team_id,
    t.team_code,
    t.team_name,
    t.condition_trpg,
    t.condition_database,
    t.condition_group,
    t.team_status
FROM dbo.Team AS t
INNER JOIN dbo.Research_Project AS rp
    ON rp.project_id = t.project_id
ORDER BY rp.project_code, t.team_code;
```

## 10. 查 PC 與 NPC

```sql
SELECT
    t.team_code,
    tm.member_code,
    pc.character_id,
    pc.character_code,
    pc.character_name,
    pc.archetype,
    pc.character_status
FROM dbo.Player_Character AS pc
INNER JOIN dbo.Team_Member AS tm
    ON tm.team_member_id = pc.team_member_id
INNER JOIN dbo.Team AS t
    ON t.team_id = tm.team_id
ORDER BY t.team_code, pc.character_code;

SELECT
    t.team_code,
    npc.npc_id,
    npc.npc_code,
    npc.npc_name,
    npc.npc_type,
    npc.role_in_story,
    npc.location_name,
    npc.npc_status
FROM dbo.NPC AS npc
INNER JOIN dbo.Team AS t
    ON t.team_id = npc.team_id
ORDER BY t.team_code, npc.npc_code;
```

## 11. 查場次、場景與逐字稿

```sql
SELECT
    t.team_code,
    s.session_id,
    s.session_code,
    s.session_no,
    s.session_title,
    s.session_type,
    s.session_date,
    s.transcript_status,
    s.session_status
FROM dbo.TRPG_Session AS s
INNER JOIN dbo.Team AS t
    ON t.team_id = s.team_id
ORDER BY t.team_code, s.session_no;

SELECT
    t.team_code,
    s.session_code,
    sc.scene_id,
    sc.scene_code,
    sc.scene_no,
    sc.scene_title,
    sc.scene_type,
    sc.start_turn_no,
    sc.end_turn_no,
    sc.review_status
FROM dbo.Scene AS sc
INNER JOIN dbo.TRPG_Session AS s
    ON s.session_id = sc.session_id
INNER JOIN dbo.Team AS t
    ON t.team_id = s.team_id
ORDER BY t.team_code, s.session_no, sc.scene_no;

SELECT TOP (200)
    t.team_code,
    s.session_code,
    sc.scene_code,
    u.turn_no,
    u.sub_turn_no,
    u.utterance_code,
    u.speaker_type,
    COALESCE(tm.member_code, pc.character_code, npc.npc_code, u.speaker_label_raw) AS speaker_code,
    u.utterance_function,
    u.is_in_character,
    u.is_gm_narration,
    u.is_rule_related,
    u.is_decision_related,
    u.is_knowledge_related,
    u.emotion_label,
    u.utterance_text_raw
FROM dbo.Utterance AS u
INNER JOIN dbo.TRPG_Session AS s
    ON s.session_id = u.session_id
INNER JOIN dbo.Team AS t
    ON t.team_id = s.team_id
LEFT JOIN dbo.Scene AS sc
    ON sc.scene_id = u.scene_id
LEFT JOIN dbo.Team_Member AS tm
    ON tm.team_member_id = u.speaker_member_id
LEFT JOIN dbo.Player_Character AS pc
    ON pc.character_id = u.speaker_character_id
LEFT JOIN dbo.NPC AS npc
    ON npc.npc_id = u.speaker_npc_id
ORDER BY t.team_code, s.session_no, u.turn_no, u.sub_turn_no;
```

## 12. 查劇情事件、決策與知識查詢

```sql
SELECT
    t.team_code,
    s.session_code,
    sc.scene_code,
    pe.event_no,
    pe.event_code,
    pe.event_title,
    pe.event_type,
    pe.event_function,
    pe.event_importance,
    pe.event_summary
FROM dbo.Plot_Event AS pe
INNER JOIN dbo.TRPG_Session AS s
    ON s.session_id = pe.session_id
INNER JOIN dbo.Team AS t
    ON t.team_id = s.team_id
LEFT JOIN dbo.Scene AS sc
    ON sc.scene_id = pe.scene_id
ORDER BY t.team_code, s.session_no, pe.event_no;

SELECT
    t.team_code,
    s.session_code,
    sc.scene_code,
    d.decision_no,
    d.decision_code,
    d.decision_title,
    d.decision_type,
    d.decision_status,
    d.consensus_level,
    d.consensus_quality_score,
    d.selected_option,
    d.decision_summary
FROM dbo.Decision_Log AS d
INNER JOIN dbo.TRPG_Session AS s
    ON s.session_id = d.session_id
INNER JOIN dbo.Team AS t
    ON t.team_id = s.team_id
LEFT JOIN dbo.Scene AS sc
    ON sc.scene_id = d.scene_id
ORDER BY t.team_code, s.session_no, d.decision_no;

SELECT
    t.team_code,
    s.session_code,
    sc.scene_code,
    k.retrieval_no,
    k.retrieval_code,
    k.retrieval_type,
    k.retrieval_purpose,
    k.query_initiator_role,
    COALESCE(tm.member_code, pc.character_code) AS initiator_code,
    k.retrieval_source_type,
    k.search_keyword,
    k.retrieval_success,
    k.retrieval_success_level,
    k.tms_role,
    k.result_used_summary
FROM dbo.Knowledge_Retrieval_Log AS k
INNER JOIN dbo.TRPG_Session AS s
    ON s.session_id = k.session_id
INNER JOIN dbo.Team AS t
    ON t.team_id = s.team_id
LEFT JOIN dbo.Scene AS sc
    ON sc.scene_id = k.scene_id
LEFT JOIN dbo.Team_Member AS tm
    ON tm.team_member_id = k.query_initiator_member_id
LEFT JOIN dbo.Player_Character AS pc
    ON pc.character_id = k.query_initiator_character_id
ORDER BY t.team_code, s.session_no, k.retrieval_no;
```

## 13. 查 staging 匯入狀態

```sql
DECLARE @batch_code NVARCHAR(100) = N'TRPG_UTT_20260505_T01_S01_v01';

SELECT
    import_batch_id,
    batch_code,
    project_code,
    source_table_name,
    source_file_name,
    import_status,
    total_row_count,
    valid_row_count,
    invalid_row_count,
    warning_count,
    error_count
FROM stg.Import_Batch
WHERE batch_code = @batch_code;

SELECT
    ui.source_row_no,
    ui.project_code,
    ui.team_code,
    ui.session_code,
    ui.scene_code,
    ui.turn_no_text,
    ui.speaker_type,
    ui.speaker_code,
    ui.utterance_function,
    ui.import_status,
    ui.validation_error,
    ui.validation_warning
FROM stg.Utterance_Import AS ui
INNER JOIN stg.Import_Batch AS ib
    ON ib.import_batch_id = ui.import_batch_id
WHERE ib.batch_code = @batch_code
ORDER BY ui.source_row_no;
```

## 14. 驗證與載入 staging

```sql
EXEC stg.usp_Validate_Utterance_Import
    @batch_code = N'TRPG_UTT_20260505_T01_S01_v01';

EXEC stg.usp_Load_Utterance_Import_To_Dbo
    @batch_code = N'TRPG_UTT_20260505_T01_S01_v01';
```

## 15. 匯出 da_go 世界資料與 runtime bundle

```sql
EXEC dbo.usp_Export_DaGo_World_Manifest
    @project_code = N'PROJECT001',
    @team_code = N'TEAM001',
    @session_code = N'SESSION001';

EXEC dbo.usp_Export_DaGo_Runtime_Bundle
    @project_code = N'PROJECT001',
    @team_code = N'TEAM001',
    @session_code = N'SESSION001';
```

## 16. 自我檢查清單

```text
1. HTML 不含 v13 / V13。
2. Console 可看到 turbo chunk v3 與 rowops v3。
3. DOCX 匯入後 workspaceStatus 顯示 TURBO_CHUNK。
4. 50,000 rows 形成 10 chunks。
5. row 操作上插、下插、分割、刪除可用。
6. Speaker Mapping 套用後，speaker_code 不回退。
7. 前端匯出 JSON / XLSX / TSV 可下載。
8. SQL Server staging 驗證程序可執行。
9. 修改 schema 前已備份資料庫與前端匯出檔。
```

## 17. 參考文獻

Microsoft. (2026, April 28). *Release notes for SQL Server Management Studio (SSMS)*. Microsoft Learn. https://learn.microsoft.com/en-us/ssms/release-notes-22

Microsoft. (2025, July 15). *Run Transact-SQL commands with the sqlcmd utility*. Microsoft Learn. https://learn.microsoft.com/en-us/sql/tools/sqlcmd/sqlcmd-utility
