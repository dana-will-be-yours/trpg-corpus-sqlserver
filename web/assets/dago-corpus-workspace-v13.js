(()=>{'use strict';
const VERSION='2026-05-12 json-xlsx-v13-indexeddb-large-workspace';
const DB_NAME='dagoCorpusWorkspaceDB';
const DB_VERSION=1;
const SESSION_KEY='dagoCorpusWorkspaceCurrentV13';
let dbPromise=null;
function now(){return new Date().toISOString()}
function safeId(s){return String(s||'workspace').replace(/[^A-Za-z0-9_-]+/g,'_').slice(0,80)}
function makeWorkspaceId(meta={}){const base=safeId(meta.batch_code||meta.session_code||'DAGO_WORKSPACE');const stamp=new Date().toISOString().replace(/[-:.TZ]/g,'').slice(0,14);const rand=Math.random().toString(36).slice(2,8);return `${base}_${stamp}_${rand}`}
function req(request){return new Promise((resolve,reject)=>{request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error||new Error('IndexedDB request failed'))})}
function txDone(tx){return new Promise((resolve,reject)=>{tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error||new Error('IndexedDB transaction failed'));tx.onabort=()=>reject(tx.error||new Error('IndexedDB transaction aborted'))})}
function openWorkspaceDb(){if(dbPromise)return dbPromise;dbPromise=new Promise((resolve,reject)=>{if(!('indexedDB' in window)){reject(new Error('此瀏覽器不支援 IndexedDB。'));return}const open=indexedDB.open(DB_NAME,DB_VERSION);open.onupgradeneeded=()=>{const db=open.result;if(!db.objectStoreNames.contains('workspaces'))db.createObjectStore('workspaces',{keyPath:'workspace_id'});if(!db.objectStoreNames.contains('rows')){const rows=db.createObjectStore('rows',{keyPath:'id'});rows.createIndex('workspace_id','workspace_id',{unique:false});rows.createIndex('workspace_source_row',['workspace_id','source_row_no'],{unique:false});rows.createIndex('workspace_speaker',['workspace_id','speaker_code'],{unique:false});rows.createIndex('workspace_row_order',['workspace_id','row_order'],{unique:false})}if(!db.objectStoreNames.contains('maps')){const maps=db.createObjectStore('maps',{keyPath:'id'});maps.createIndex('workspace_id','workspace_id',{unique:false});maps.createIndex('workspace_raw_speaker',['workspace_id','raw_speaker_label'],{unique:false})}};open.onsuccess=()=>resolve(open.result);open.onerror=()=>reject(open.error||new Error('IndexedDB open failed'))});return dbPromise}
function setCurrentWorkspace(workspace_id){sessionStorage.setItem(SESSION_KEY,JSON.stringify({workspace_id,version:VERSION,updated_at:now()}));return workspace_id}
function getCurrentWorkspaceId(){try{return JSON.parse(sessionStorage.getItem(SESSION_KEY)||'{}').workspace_id||null}catch(e){return null}}
function rowRange(workspace_id){return IDBKeyRange.bound([workspace_id,Number.NEGATIVE_INFINITY],[workspace_id,Number.POSITIVE_INFINITY])}
function speakerRange(workspace_id,speaker){return IDBKeyRange.only([workspace_id,speaker])}
async function withTransaction(storeNames,mode='readonly',callback=()=>{}){const db=await openWorkspaceDb();const names=Array.isArray(storeNames)?storeNames:[storeNames];const tx=db.transaction(names,mode);const stores=Object.fromEntries(names.map(name=>[name,tx.objectStore(name)]));let result;try{result=callback(stores,tx)}catch(e){try{tx.abort()}catch(ignore){}throw e}await txDone(tx);return result}
async function cursorRecords(storeName,indexName,range,options={},callback=()=>{}){const db=await openWorkspaceDb();const tx=db.transaction(storeName,'readonly');const source=indexName?tx.objectStore(storeName).index(indexName):tx.objectStore(storeName);const direction=options.direction||'next';const limit=Number(options.limit||0);const offset=Number(options.offset||0);const out=[];const reqCur=source.openCursor(range,direction);let skipped=false,seen=0;await new Promise((resolve,reject)=>{reqCur.onerror=()=>reject(reqCur.error);reqCur.onsuccess=e=>{const c=e.target.result;if(!c||limit&&out.length>=limit){resolve();return}if(!skipped&&offset>0){skipped=true;c.advance(offset);return}const value=c.value;const mapped=callback(value,seen++,c);if(mapped!==undefined)out.push(mapped);c.continue()}});return out}
async function cursorRows(workspace_id=getCurrentWorkspaceId(),options={},callback){if(!workspace_id)return[];const speaker=options.speakerFilter||options.speaker_code||'__ALL__';const limit=Number(options.limit||0);const offset=Number(options.offset||0);if(speaker&&speaker!=='__ALL__'){const rows=await cursorRecords('rows','workspace_speaker',speakerRange(workspace_id,speaker),{limit:0,offset:0},(rec)=>rec);const sorted=rows.sort((a,b)=>(a.row_order-b.row_order)||(a.source_row_no-b.source_row_no));const sliced=limit?sorted.slice(offset,offset+limit):sorted.slice(offset);return sliced.map((rec,i)=>callback?callback(rec.row_json,i,rec):rec.row_json)}return await cursorRecords('rows','workspace_row_order',rowRange(workspace_id),{limit,offset},(rec,i)=>callback?callback(rec.row_json,i,rec):rec.row_json)}
async function updateRowsByCursor(workspace_id=getCurrentWorkspaceId(),updater=()=>null){if(!workspace_id)return 0;const db=await openWorkspaceDb();const tx=db.transaction('rows','readwrite');const idx=tx.objectStore('rows').index('workspace_row_order');const reqCur=idx.openCursor(rowRange(workspace_id));let count=0;await new Promise((resolve,reject)=>{reqCur.onerror=()=>reject(reqCur.error);reqCur.onsuccess=e=>{const c=e.target.result;if(!c){resolve();return}try{const rec=c.value;const nextRow=updater(rec.row_json,count,rec);if(nextRow){const source=Number(nextRow.source_row_no||rec.source_row_no);const updated={...rec,source_row_no:source,row_order:Number(nextRow.row_order||rec.row_order||source),speaker_code:nextRow.speaker_code||'',speaker_type:nextRow.speaker_type||'',turn_no_text:String(nextRow.turn_no_text||source),row_json:{...nextRow,source_row_no:source,row_order:Number(nextRow.row_order||rec.row_order||source),turn_no_text:String(nextRow.turn_no_text||source)},updated_at:now()};c.update(updated)}count++;c.continue()}catch(err){reject(err)}}});await txDone(tx);return count}
async function getRowsChunkByCursor(workspace_id=getCurrentWorkspaceId(),startIndex=0,limit=1000,speakerFilter='__ALL__'){return await cursorRows(workspace_id,{offset:Number(startIndex)||0,limit:Number(limit)||1000,speakerFilter})}
async function countRows(workspace_id=getCurrentWorkspaceId(),speakerFilter='__ALL__'){return await getRowCount(workspace_id,speakerFilter)}
async function createWorkspace(metadata={}){const db=await openWorkspaceDb();const workspace_id=metadata.workspace_id||makeWorkspaceId(metadata);const record={workspace_id,version_name:VERSION,project_code:metadata.project_code||'',team_code:metadata.team_code||'',session_code:metadata.session_code||'',batch_code:metadata.batch_code||'',source_file_name:metadata.source_file_name||'',source_url:metadata.source_url||'',total_row_count:metadata.total_row_count||0,last_page_no:1,last_page_size:metadata.last_page_size||100,last_speaker_filter:'__ALL__',created_at:now(),updated_at:now()};const tx=db.transaction('workspaces','readwrite');tx.objectStore('workspaces').put(record);await txDone(tx);setCurrentWorkspace(workspace_id);return record}
async function getWorkspace(workspace_id=getCurrentWorkspaceId()){if(!workspace_id)return null;const db=await openWorkspaceDb();return await req(db.transaction('workspaces','readonly').objectStore('workspaces').get(workspace_id))}
async function saveWorkspaceMetadata(workspace){if(!workspace||!workspace.workspace_id)throw new Error('workspace_id 必填');const db=await openWorkspaceDb();workspace.updated_at=now();const tx=db.transaction('workspaces','readwrite');tx.objectStore('workspaces').put(workspace);await txDone(tx);setCurrentWorkspace(workspace.workspace_id);return workspace}
function normalizeRow(workspace_id,row,index){const source=Number(row.source_row_no||index+1);const order=Number(row.row_order||source);return{id:`${workspace_id}:${source}`,workspace_id,source_row_no:source,row_order:order,speaker_code:row.speaker_code||'',speaker_type:row.speaker_type||'',turn_no_text:String(row.turn_no_text||source),row_json:{...row,source_row_no:source,row_order:order,turn_no_text:String(row.turn_no_text||source)},updated_at:now()}}
async function saveRows(workspace_id,rows=[]){if(!workspace_id)throw new Error('workspace_id 必填');const db=await openWorkspaceDb();const tx=db.transaction(['rows','workspaces'],'readwrite');const store=tx.objectStore('rows');rows.forEach((row,i)=>store.put(normalizeRow(workspace_id,row,i)));const wsStore=tx.objectStore('workspaces');const ws=await new Promise((resolve,reject)=>{const r=wsStore.get(workspace_id);r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});if(ws){ws.total_row_count=rows.length;ws.updated_at=now();wsStore.put(ws)}await txDone(tx);return rows.length}
async function saveMaps(workspace_id,maps=[]){if(!workspace_id)throw new Error('workspace_id 必填');const db=await openWorkspaceDb();const tx=db.transaction('maps','readwrite');const store=tx.objectStore('maps');maps.forEach((map,i)=>{const raw=map.raw_speaker_label||`map_${i+1}`;store.put({id:`${workspace_id}:${raw}`,workspace_id,raw_speaker_label:raw,map_json:{...map},updated_at:now()})});await txDone(tx);return maps.length}
async function getAllMaps(workspace_id=getCurrentWorkspaceId()){if(!workspace_id)return[];const db=await openWorkspaceDb();const idx=db.transaction('maps','readonly').objectStore('maps').index('workspace_id');const list=await req(idx.getAll(workspace_id));return list.map(x=>x.map_json)}
async function getAllRows(workspace_id=getCurrentWorkspaceId()){if(!workspace_id)return[];const out=[];await iterateRows(workspace_id,(row)=>{out.push(row)});return out}
async function getRowCount(workspace_id=getCurrentWorkspaceId(),speakerFilter='__ALL__'){if(!workspace_id)return 0;const db=await openWorkspaceDb();const tx=db.transaction('rows','readonly');if(speakerFilter&&speakerFilter!=='__ALL__')return await req(tx.objectStore('rows').index('workspace_speaker').count(speakerRange(workspace_id,speakerFilter)));return await req(tx.objectStore('rows').index('workspace_id').count(workspace_id))}
async function getRowsPage(workspace_id=getCurrentWorkspaceId(),pageNo=1,pageSize=100,speakerFilter='__ALL__'){if(!workspace_id)return[];const ps=Number(pageSize)||100;if(ps===0)return await getAllRows(workspace_id);const targetStart=(Math.max(1,Number(pageNo)||1)-1)*ps;const out=[];const db=await openWorkspaceDb();const tx=db.transaction('rows','readonly');if(speakerFilter&&speakerFilter!=='__ALL__'){const idx=tx.objectStore('rows').index('workspace_speaker');const reqCur=idx.openCursor(speakerRange(workspace_id,speakerFilter));let seen=0;await new Promise((resolve,reject)=>{reqCur.onerror=()=>reject(reqCur.error);reqCur.onsuccess=e=>{const c=e.target.result;if(!c||out.length>=ps){resolve();return}const rec=c.value;if(seen++>=targetStart)out.push(rec);c.continue()}});return out.sort((a,b)=>(a.row_order-b.row_order)||(a.source_row_no-b.source_row_no)).map(x=>x.row_json)}const idx=tx.objectStore('rows').index('workspace_row_order');const reqCur=idx.openCursor(rowRange(workspace_id));let skipped=false;await new Promise((resolve,reject)=>{reqCur.onerror=()=>reject(reqCur.error);reqCur.onsuccess=e=>{const c=e.target.result;if(!c||out.length>=ps){resolve();return}if(!skipped&&targetStart>0){skipped=true;c.advance(targetStart);return}out.push(c.value);c.continue()}});return out.map(x=>x.row_json)}
async function getSpeakerCounts(workspace_id=getCurrentWorkspaceId()){if(!workspace_id)return[];const counts=new Map();const db=await openWorkspaceDb();const idx=db.transaction('rows','readonly').objectStore('rows').index('workspace_id');const reqCur=idx.openCursor(workspace_id);await new Promise((resolve,reject)=>{reqCur.onerror=()=>reject(reqCur.error);reqCur.onsuccess=e=>{const c=e.target.result;if(!c){resolve();return}const r=c.value.row_json||{};const key=r.speaker_code||r.speaker_label_raw||'未設定';const label=`${key}${r.speaker_label_raw&&r.speaker_label_raw!==key?'｜'+r.speaker_label_raw:''}`;if(!counts.has(key))counts.set(key,{speaker_code:key,label,count:0});counts.get(key).count++;c.continue()}});return Array.from(counts.values()).sort((a,b)=>a.speaker_code.localeCompare(b.speaker_code,'zh-Hant'))}
async function updateRow(workspace_id,source_row_no,row){if(!workspace_id)throw new Error('workspace_id 必填');const db=await openWorkspaceDb();const id=`${workspace_id}:${Number(source_row_no)}`;const tx=db.transaction('rows','readwrite');const store=tx.objectStore('rows');const old=await new Promise((resolve,reject)=>{const r=store.get(id);r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});const record=normalizeRow(workspace_id,{...(old?.row_json||{}),...row,source_row_no:Number(source_row_no)},Number(source_row_no)-1);record.id=id;record.row_order=old?.row_order||record.row_order;store.put(record);await txDone(tx);return record.row_json}
async function deleteRow(workspace_id,source_row_no){const db=await openWorkspaceDb();const tx=db.transaction('rows','readwrite');tx.objectStore('rows').delete(`${workspace_id}:${Number(source_row_no)}`);await txDone(tx)}
async function clearWorkspace(workspace_id=getCurrentWorkspaceId()){if(!workspace_id)return;const db=await openWorkspaceDb();const tx=db.transaction(['workspaces','rows','maps'],'readwrite');tx.objectStore('workspaces').delete(workspace_id);for(const storeName of ['rows','maps']){const store=tx.objectStore(storeName);const idx=store.index('workspace_id');const range=IDBKeyRange.only(workspace_id);idx.openKeyCursor(range).onsuccess=e=>{const cursor=e.target.result;if(cursor){store.delete(cursor.primaryKey);cursor.continue()}}}await txDone(tx);try{if(getCurrentWorkspaceId()===workspace_id)sessionStorage.removeItem(SESSION_KEY)}catch(e){}}
async function clearAllWorkspaces(){const db=await openWorkspaceDb();const tx=db.transaction(['workspaces','rows','maps'],'readwrite');['workspaces','rows','maps'].forEach(n=>tx.objectStore(n).clear());await txDone(tx);sessionStorage.removeItem(SESSION_KEY)}
async function iterateRows(workspace_id=getCurrentWorkspaceId(),callback=()=>{}){if(!workspace_id)return;const db=await openWorkspaceDb();const idx=db.transaction('rows','readonly').objectStore('rows').index('workspace_row_order');const reqCur=idx.openCursor(rowRange(workspace_id));let i=0;await new Promise((resolve,reject)=>{reqCur.onerror=()=>reject(reqCur.error);reqCur.onsuccess=async e=>{const c=e.target.result;if(!c){resolve();return}try{await callback(c.value.row_json,i++,c.value)}catch(err){reject(err);return}c.continue()}})}
async function updateWorkspaceStatusElement(){const el=document.getElementById('workspaceStatus');if(!el)return;try{const workspace_id=getCurrentWorkspaceId();if(!workspace_id){el.textContent='IndexedDB 工作區：尚未建立';return}const ws=await getWorkspace(workspace_id);const count=await getRowCount(workspace_id);el.textContent=`IndexedDB 工作區：${workspace_id}；rows=${count}；版本=${VERSION}`;if(ws&&ws.total_row_count!==count){ws.total_row_count=count;await saveWorkspaceMetadata(ws)}}catch(e){el.textContent='IndexedDB 工作區錯誤：'+e.message}}
function bindBasicUi(){const btn=document.getElementById('clearIndexedDbWorkspace');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=async()=>{if(!confirm('確定清除目前本機 IndexedDB 工作區？此動作不會刪除 SQL Server 資料。'))return;try{await clearWorkspace();await updateWorkspaceStatusElement();const s=document.getElementById('status');if(s)s.textContent='已清除目前本機 IndexedDB 工作區。'}catch(e){const s=document.getElementById('status');if(s)s.textContent='清除 IndexedDB 工作區失敗：'+e.message}}}updateWorkspaceStatusElement()}
window.DagoCorpusWorkspaceV13={VERSION,DB_NAME,SESSION_KEY,openWorkspaceDb,createWorkspace,setCurrentWorkspace,getCurrentWorkspaceId,getWorkspace,saveWorkspaceMetadata,saveRows,saveMaps,getAllMaps,getAllRows,getRowCount,getRowsPage,getSpeakerCounts,updateRow,deleteRow,clearWorkspace,clearAllWorkspaces,iterateRows,withTransaction,cursorRecords,cursorRows,updateRowsByCursor,getRowsChunkByCursor,countRows,updateWorkspaceStatusElement};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bindBasicUi);else bindBasicUi();
})();